// Drop your RFF_Retirement_Calculator.jsx into src/ and this import will pick it up.
// If the calculator's default export uses a different name, the import below still works
// because we're importing the default export.
import RetirementCalculator from './RFF_Retirement_Calculator.jsx'

export default function App() {
  return <RetirementCalculator />
}
